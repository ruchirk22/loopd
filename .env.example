# Copy to .env or export these in your shell. Only ANTHROPIC_API_KEY is required.

# Auth for headless Claude Code (get one from the Anthropic Console).
ANTHROPIC_API_KEY=sk-ant-...

# --- Optional overrides (defaults shown) ---
# PM_MODEL=opus
# DEV_MODEL=sonnet
# DEV_ALLOWED_TOOLS=Read,Edit,Write,Bash,Glob,Grep
# PM_ALLOWED_TOOLS=Read,Glob,Grep
# MAX_ATTEMPTS_PER_STEP=3
# MAX_TURNS_PER_CALL=40
# BUDGET_USD=10
# DEV_PERMISSION_MODE=bypassPermissions
